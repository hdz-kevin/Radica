---
paths:
  - 'app/**'
---

# App

## MVP is built in teaching slices
Canonical spec: docs/mvp_plan.md. Build one numbered slice at a time (data, catalog, publish, photos, filters, map, OAuth, admin). After each slice, explain architecture and Laravel/backend concepts for Kevin. Do not skip to Mapbox or Socialite before the listing domain exists. Do not implement the full MVP in one pass.
